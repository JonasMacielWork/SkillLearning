apiVersion: v1
kind: Service
metadata:
  name: xray-daemon
  labels:
    app: xray-daemon
spec:
  ports:
    - name: "2000"
      port: 2000
      targetPort: 2000
      protocol: UDP
  selector:
    app: xray-daemon