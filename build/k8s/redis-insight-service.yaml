apiVersion: v1
kind: Service
metadata:
  name: redis-insight
  labels:
    app: redis-insight
spec:
  type: LoadBalancer
  ports:
    - name: "8081"
      port: 8081
      targetPort: 5540
  selector:
    app: redis-insight