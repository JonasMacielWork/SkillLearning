apiVersion: v1
kind: Service
metadata:
  name: skilllearning-api
  labels:
    app: skilllearning-api
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 5000
      targetPort: 8080
  selector:
    app: skilllearning-api